{
  "enabled": true,
  "_enabled": "Master switch — when false, this plugin is skipped during indexing.",
  "extensions": ["Dockerfile", ".dockerfile"],
  "_extensions": "File extensions/patterns this plugin claims.",
  "filenames": ["Dockerfile", "dockerfile"],
  "_filenames": "Exact filename matches to parse."
}
