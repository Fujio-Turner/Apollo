{
  "enabled": true,
  "_enabled": "Master switch — when false, this plugin is skipped during indexing and Node.js files fall through to the generic text indexer.",
  "extensions": [".js", ".mjs", ".cjs"],
  "_extensions": "File extensions this plugin claims (lower-case, leading dot).",
  "extract_comments": true,
  "_extract_comments": "Extract `// TODO`-style comment nodes from each file.",
  "comment_tags": ["TODO", "FIXME", "NOTE", "HACK", "XXX", "WARN"],
  "_comment_tags": "Tags surfaced as comment nodes when `extract_comments` is true.",
  "extract_calls": true,
  "_extract_calls": "Walk function/method bodies for callsites so the graph can draw `calls` edges. Disable on very large codebases for faster indexing.",
  "ignore_dirs": [
    "node_modules",
    ".npm",
    ".yarn",
    "dist",
    "build",
    "coverage",
    ".next",
    ".nuxt",
    "out"
  ],
  "_ignore_dirs": "Folder names the indexer skips when this plugin is enabled.",
  "ignore_files": [
    "*.min.js",
    "*.map",
    "*.lock"
  ],
  "_ignore_files": "Glob patterns for files the indexer should skip."
}
