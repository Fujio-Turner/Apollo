networkx>=3.0
mistune>=3.0
python-frontmatter>=1.0
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
numpy
# Optional fast JSON encoder used by storage/json_store.py for index
# (de)serialization. ~3–5× faster than stdlib json with smaller output;
# the store falls back to stdlib json when this isn't installed.
orjson>=3.9
sentence-transformers
openai>=1.0.0
# TOON (Token-Oriented Object Notation) — encodes tool results sent back to
# the LLM in a CSV/YAML-hybrid format that uses 30-60% fewer tokens than JSON
# for tabular results. Lets the model fit more tool rounds before hitting the
# context limit. See chat/service.py:_to_toon_for_llm.
python-toon>=0.1.3
watchdog>=3.0.0
pytest>=7.0
jsonschema>=4.0
python-ulid>=1.0
# Tree-sitter multi-language parser (optional — Step 7)
tree-sitter>=0.23.0,<0.24
tree-sitter-python>=0.23
tree-sitter-javascript>=0.23
tree-sitter-typescript>=0.23
tree-sitter-go>=0.23
tree-sitter-rust>=0.23,<0.24

# ─── ML extras (PLAN_ML_LIBS.md) ─────────────────────────────────────
# All optional. The indexer's ML passes (`ml/passes.py`) and the
# matching chat tools (`list_clusters`, `get_node_importance`,
# `find_outliers`, `get_topics`, `find_dead_code`, ...) skip cleanly
# with `ml_available=false` when these aren't installed. Install only
# what you want; the per-pass cost is documented in
# `docs/work/PLAN_ML_LIBS.md` §3.6.
#
# Phase 1 (UMAP layout, HDBSCAN clusters, KeyBERT keyphrases — ~80 MB):
#   pip install umap-learn hdbscan keybert
# Phase 2 (Louvain communities — ~5 MB; falls back to NetworkX
# greedy_modularity_communities when missing):
#   pip install python-louvain
# Phase 3 (vulture dead-code — ~1 MB):
#   pip install vulture
# Phase 4 (BERTopic auto-labelled topics — ~30 MB):
#   pip install bertopic
# scikit-learn (already pulled in by sentence-transformers) is the
# Phase 3.1 IsolationForest backend — no extra install needed.
#
# These intentionally are NOT in the always-required list to keep the
# default install lean. Uncomment any you want bundled by default:
# umap-learn>=0.5
# hdbscan>=0.8
# keybert>=0.8
# python-louvain>=0.16
# vulture>=2.10
# bertopic>=0.16
